name: gpml_preprocess
# Data loading, dbNSFP score integration, and training set flagging.
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pandas=1.4
  - numpy=1.23
  - scipy=1.9
