name: gpml_correlation
# Spearman correlation calculations between MAVE scores and prediction tools.
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pandas=1.4
  - numpy=1.23
  - scipy=1.9
  - statsmodels=0.13
